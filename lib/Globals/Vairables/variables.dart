enum PAGE { speakerDept, seeratUnNabi }
// enum FUNCTION { speaker, visitor, student, follow, finance, faq, orientation }

class Variables {
  static int PageNo = 0;
  static DateTime CURRENTBACKPRESSTIME;
}
